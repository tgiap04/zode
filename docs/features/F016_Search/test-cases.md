# Test Cases — F016_Search

**SIDECAR (v26.1.0):** this is a 5th, optional file alongside the 4 mandatory feature-spec files.
Its absence never blocks feature-spec promotion.

**Code Format**: `TC###` — 3-digit zero-padded, **resets per feature** (this file's own scope is
the reset boundary; unlike `JOB###`, which is file-global).

**Citation-source split**: `UT`/`IT` rows cite a `BR-###`/`SM-###`/`DEC-###`/`DISC-###` code, a
`` `file:line` ``, or an `edge-cases.md` row. `UAT` rows cite a `screens.md`/`business-context.md`
section — NEVER a bare code (UAT is less code-traceable by design; see
`references/test-cases-researcher-contract.md`).

**CSV export**: out of scope v1 — Markdown is the sole output. See plan Decision.

---

## Test Cases

| Test-ID | Type (UT\|IT\|UAT) | Given                                                                                                           | When                                                                                                                                    | Then                                                                                                                                                                                                                     | Traces-to                                                                   |
| ------- | ------------------ | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------- |
| TC001   | UT                 | A project has more than 100 files matching the typed fuzzy query                                                | The developer's query is run through `FileFinderDelegate::spawn_search`                                                                 | The picker computes/returns at most 100 ranked matches; no result beyond rank 100 is ever computed                                                                                                                       | `BR-001`                                                                    |
| TC002   | UT                 | Case-sensitive is off and the typed query contains a non-ASCII character                                        | `SearchQuery::text` builds the matcher for that query                                                                                   | The query is transparently re-dispatched to `SearchQuery::regex` (literal-escaped, `multiline=false`), and the non-ASCII text still matches                                                                              | `BR-002`                                                                    |
| TC003   | IT                 | A project-wide search is running and accumulating matched files/ranges                                          | The running total crosses 5,000 matched files or 10,000 total ranges                                                                    | A `SearchResult::LimitReached` marker stops further accumulation; `ProjectSearchView` sets `limit_reached = true` and renders the match counter as `"{index}/{count}+"`                                                  | `BR-003`                                                                    |
| TC004   | UT                 | The buffer search bar is in regex mode with an existing set of highlighted matches                              | The developer types a query that fails to compile as a regex                                                                            | `BufferSearchBar::update_matches` sets `query_error` to the compile-error string, clears the active searchable item's highlighted matches, and returns without running a search or crashing                              | `BR-004`                                                                    |
| TC005   | IT                 | The `command_invocations` table currently holds 1,000 rows                                                      | The developer confirms one more command in the command palette                                                                          | `CommandPaletteDB::write_command_invocation_internal` inserts the new row (command_name, user_query, `last_invoked=unixepoch()`) then deletes the row(s) with the minimum `id`, keeping the table capped near 1,000 rows | `BR-005`                                                                    |
| TC006   | UT                 | Whole-word mode is enabled and the buffer contains a candidate match whose start or end boundary falls mid-word | `SearchQuery`'s stream scan (`AhoCorasick`/`Regex`) reaches that candidate                                                              | The candidate is skipped via the `CharKind` boundary check on both edges and is not added to the returned `Vec<Range<usize>>`                                                                                            | `crates/project/src/search.rs:427-479` (ALG-001)                            |
| TC007   | UT                 | The typed fuzzy query in the file finder matches no existing file and doesn't end in a path separator           | The picker finishes ranking                                                                                                             | A `Match::CreateNew` candidate for that exact path is appended and always sorted last in the list                                                                                                                        | edge-cases.md § "File finder query matches no existing file"                |
| TC008   | UT                 | A recent-history candidate's on-disk file path has been deleted since it was recorded                           | `FileFinderDelegate::new`'s background `fs.is_file` check runs                                                                          | The stale entry is dropped from the candidate list before it can ever be offered as a match                                                                                                                              | edge-cases.md § "A recent-history entry's file no longer exists on disk"    |
| TC009   | IT                 | The project search Query field is in regex mode                                                                 | The developer submits a query that fails to compile as a valid regex                                                                    | The Query input panel is flagged in `panels_with_errors`, no search dispatches, and the flag clears once the query becomes valid again                                                                                   | edge-cases.md § "Project search query is an invalid regex (regex mode on)"  |
| TC010   | UT                 | The buffer search bar currently has highlighted matches from a prior query                                      | The developer clears the query field to empty                                                                                           | All existing highlighted matches are cleared and no search is run                                                                                                                                                        | edge-cases.md § "Buffer search query is left empty"                         |
| TC011   | IT                 | A command palette selection has just been confirmed and dispatched                                              | The background write to `command_invocations` fails with a SQLite error                                                                 | The error is logged internally (not surfaced to the user) and the confirmed action still executes normally                                                                                                               | edge-cases.md § "Command-palette invocation log write fails (SQLite error)" |
| TC012   | UAT                | The developer wants to open a known file without navigating the project tree                                    | They press the file-finder keybinding and type a fuzzy fragment of the filename, then confirm the top-ranked result                     | The file finder modal appears with ranked matches, and confirming opens that file in the active pane                                                                                                                     | screens.md § User Journey step 1                                            |
| TC013   | UAT                | The developer's typed fragment matches no existing file                                                         | They confirm the "create new file" entry shown at the bottom of the list                                                                | That exact path is created and opened, instead of leaving the developer with an empty list                                                                                                                               | screens.md § User Journey step 2                                            |
| TC014   | UAT                | The developer wants every usage of a term across the project                                                    | They open project search and submit a query                                                                                             | Matches stream in grouped by file with surrounding context; if the project is large enough to hit the result cap, the count is shown suffixed with "+" instead of an exact number                                        | screens.md § User Journey step 3                                            |
| TC015   | UAT                | The developer is viewing project search results for a term                                                      | They edit a matched line directly inside the results view                                                                               | The edit is written to the real underlying file, since the results view is a live, editable multi-buffer and not a static list                                                                                           | screens.md § User Journey step 4                                            |
| TC016   | UAT                | The developer is working in an open file and wants to jump between repeated occurrences of a term               | They open the buffer search bar, type the query, cycle with Next/Previous, then expand the replace field and replace one or all matches | Matches highlight and are navigable in document order, and the chosen replace action (one or all) applies in place                                                                                                       | screens.md § User Journey step 5                                            |
| TC017   | UAT                | The developer regularly confirms commands through the command palette                                           | They confirm a command, and this repeats over many sessions                                                                             | Each confirmation is quietly logged in the background with no visible UI change, so frequently-used commands are ranked higher in future palette searches                                                                | business-context.md § What They Do (item 6)                                 |

---

## Coverage Notes

None — every `BR-###` and the one `ALG-###` code documented in `technical-spec.md` has a tracing
test case above (TC001–TC006); `SM-###`/`DEC-###`/`DISC-###` are explicitly `N/A` for this feature
(no persisted or ≥3-state machine, no multi-predicate decision logic, no discriminator-field
branch in scope), so there is nothing left in that citation family to mark `[NO_TEST_CASE]`.
