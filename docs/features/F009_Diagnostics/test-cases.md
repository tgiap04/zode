# Test Cases — F009_Diagnostics

**SIDECAR (v26.1.0):** this is a 5th, optional file alongside the 4 mandatory feature-spec files.
Its absence never blocks feature-spec promotion.

**Code Format**: `TC###` — 3-digit zero-padded, **resets per feature** (this file's own scope is
the reset boundary; unlike `JOB###`, which is file-global).

**Citation-source split**: `UT`/`IT` rows cite a `BR-###`/`SM-###`/`DEC-###`/`DISC-###` code, a
`` `file:line` ``, or an `edge-cases.md` row. `UAT` rows cite a `screens.md`/`business-context.md`
section — NEVER a bare code (UAT is less code-traceable by design; see
`references/test-cases-researcher-contract.md`).

**CSV export**: out of scope v1 — Markdown is the sole output. See plan Decision.

---

## Test Cases

| Test-ID | Type (UT\|IT\|UAT) | Given                                                                                                                                                                        | When                                                                                              | Then                                                                                                                                                                                                                                                           | Traces-to                                                                                                       |
| ------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| TC001   | IT                 | Two `DiagnosticsUpdated` events for overlapping paths arrive within the 50ms debounce window, and an `update_stale_excerpts` task is already in flight                       | The second event fires before the first task drains its queue                                     | The second event's paths are folded into the existing `paths_to_update` queue and only one rebuild task runs, not two                                                                                                                                          | `BR-001`                                                                                                        |
| TC002   | IT                 | `Project::has_stale_diagnostics` is true (local project has entries in `stale_language_servers`, or remote project has non-empty `stale_paths`) after a hibernate/wake cycle | The developer opens the project-wide diagnostics view (`ProjectDiagnosticsEditor::render`)        | A warning-styled banner ("Project re-indexing after waking — some counts may be stale") renders above the list, and the underlying counts are still shown, not hidden                                                                                          | `BR-002`                                                                                                        |
| TC003   | IT                 | `Project::is_diagnostic_summary_stale(path)` is true for one exact file path, with `diagnostic_badges` enabled and `ShowDiagnostics` not `Off`                               | The project panel recomputes `stale_diagnostic_paths` and renders that entry                      | That file's diagnostic badge/icon renders dimmed (not removed, not shown as a verified count); a sibling file at a different path with fresh diagnostics renders its badge normally                                                                            | `BR-003`                                                                                                        |
| TC004   | IT                 | A `ProjectDiagnosticsEditor` is open with warnings currently excluded (`IncludeWarnings = false`)                                                                            | The developer triggers `ToggleWarnings`                                                           | The process-global `IncludeWarnings` flips to true, every open `ProjectDiagnosticsEditor` observing the global re-derives its severity threshold to `Warning`, calls `refresh`, and warning-severity excerpts appear without affecting error-severity excerpts | `BR-004`                                                                                                        |
| TC005   | IT                 | An excerpt's source buffer has zero remaining diagnostics, is not dirty, and (with `retain_selections` true) has no active selection inside it                               | A blur, save, or selection-change event triggers `close_diagnosticless_buffers`                   | The excerpt for that buffer is removed from the aggregated multibuffer                                                                                                                                                                                         | `BR-005`                                                                                                        |
| TC006   | UT                 | The workspace's active pane item is not an `Editor`, or is an `Editor` with no resolvable `project_path` (e.g. an unsaved scratch buffer)                                    | The developer triggers `DeployCurrentFile`                                                        | The command is a silent no-op — no tab opens and no error is shown                                                                                                                                                                                             | `BR-006`                                                                                                        |
| TC007   | IT                 | The focused buffer has 3 LSP diagnostics and no `BufferDiagnosticsEditor` tab for that path is currently open                                                                | The developer triggers `DeployCurrentFile`                                                        | A new diagnostics pane opens showing exactly those 3 excerpts, no more, no fewer                                                                                                                                                                               | `crates/diagnostics/src/buffer_diagnostics.rs:212-254`                                                          |
| TC008   | IT                 | A `BufferDiagnosticsEditor` tab for the focused buffer's path is already open                                                                                                | The developer triggers `DeployCurrentFile` again for the same path                                | The existing tab is focused rather than a duplicate tab being opened                                                                                                                                                                                           | `crates/diagnostics/src/buffer_diagnostics.rs:212-254`                                                          |
| TC009   | IT                 | Two or more files across two different worktrees each have at least one LSP diagnostic                                                                                       | The developer triggers `Deploy`                                                                   | A single aggregated multibuffer lists diagnostics from every affected file across both worktrees                                                                                                                                                               | `crates/diagnostics/src/diagnostics.rs:416-447`                                                                 |
| TC010   | UT                 | `ActivityIndicator.statuses` has 2+ queued entries whose state is `Failed{error}` or `Health(Error or Warning, Some(msg))`                                                   | The developer clicks the activity indicator, invoking `show_error_message`                        | Only the first matching entry is surfaced (emitted as `ShowStatus`) and removed from `statuses`; the remaining queued entries stay queued for a subsequent click                                                                                               | `BR-007`; `crates/activity_indicator/src/activity_indicator.rs:267-296`                                         |
| TC011   | UT                 | `statuses` contains a `Health(Error, None)` entry (no message text) ahead of a `Health(Warning, Some(msg))` entry                                                            | The developer clicks the activity indicator                                                       | The `None`-message entry is dropped silently without being shown, and the next entry with an actual message is what gets surfaced                                                                                                                              | `BR-007`                                                                                                        |
| TC012   | UT                 | No formatting failure is currently recorded on the project                                                                                                                   | The developer triggers `DismissMessage`                                                           | Nothing happens — the action is a no-op, with no state change and no error                                                                                                                                                                                     | `edge-cases.md § Developer dismisses the status-bar error message when there is no recorded formatting failure` |
| TC013   | IT                 | An ETW recording session has been started via `record_etw_trace_inner`, and the `cancel_guard` defer has been registered                                                     | The function returns early (error) before an explicit `Save`/`Cancel` command is received         | `control_manager.Cancel` runs via the defer guard, releasing the kernel trace-collection buffers, and no partial/corrupt trace file is left behind                                                                                                             | `BR-008`                                                                                                        |
| TC014   | UT                 | The developer is preparing a bug report                                                                                                                                      | The developer triggers `CopySystemSpecsIntoClipboard`                                             | The clipboard contains a `SystemSpecs` JSON payload with app version, release channel, OS name/version, memory, architecture, commit SHA, and bundle type populated (GPU specs may be `None`)                                                                  | `crates/system_specs/src/system_specs.rs:20-34`                                                                 |
| TC015   | UT                 | `include_warnings` is toggled between `true` and `false`                                                                                                                     | `ALG-001` maps the boolean onto `Editor::set_max_diagnostics_severity`                            | The effective threshold is `Warning` (shows Error+Warning) when `include_warnings` is true, and `Error` (Error only) when false                                                                                                                                | `crates/project/src/project_settings.rs:342-349`                                                                |
| TC016   | IT                 | A `LanguageServerUpdate` event is forwarded through `LspStore`'s event stream with no server name attached                                                                   | `ActivityIndicator` receives the update                                                           | The update is silently skipped (`return` inside the match arm) — `statuses` is unchanged, no crash occurs, and nothing is logged                                                                                                                               | `crates/activity_indicator/src/activity_indicator.rs:123-181`                                                   |
| TC017   | IT                 | A language server's `ServerHealth` is currently `Ok`                                                                                                                         | The server reports `Health(Warning, msg)`                                                         | The state transitions `Ok -> Warning`; the status-bar label prefix changes to `"(name) Warning: "`                                                                                                                                                             | `SM-001`                                                                                                        |
| TC018   | IT                 | A language server's `ServerHealth` is currently `Ok`                                                                                                                         | The server reports `Health(Error, msg)`                                                           | The state transitions `Ok -> Error`; the status-bar label prefix changes to `"(name) Error: "`                                                                                                                                                                 | `SM-001`                                                                                                        |
| TC019   | IT                 | A language server's `ServerHealth` is currently `Warning`                                                                                                                    | The server reports `Health(Ok, _)`                                                                | The state transitions `Warning -> Ok`; the status-bar label prefix reverts to the plain `"(name) "` form                                                                                                                                                       | `SM-001`                                                                                                        |
| TC020   | IT                 | A language server's `ServerHealth` is currently `Error`                                                                                                                      | The server reports `Health(Ok, _)`                                                                | The state transitions `Error -> Ok`; the status-bar label prefix reverts to the plain `"(name) "` form                                                                                                                                                         | `SM-001`                                                                                                        |
| TC021   | IT                 | A language server's `ServerHealth` is currently `Warning`                                                                                                                    | The server reports `Health(Error, msg)`                                                           | The state transitions `Warning -> Error`; the status-bar label prefix escalates to `"(name) Error: "`                                                                                                                                                          | `SM-001`                                                                                                        |
| TC022   | UAT                | Developer is editing a file that has known LSP problems                                                                                                                      | Developer dispatches the current-file diagnostics command                                         | A tab opens (or focuses) showing only that file's diagnostic excerpts, nothing from other files                                                                                                                                                                | `screens.md § User Journey step 1`                                                                              |
| TC023   | UAT                | Developer wants the full picture across the project before wrapping up work                                                                                                  | Developer dispatches the project-wide diagnostics command                                         | A tab opens aggregating every file's diagnostics into one scrollable list                                                                                                                                                                                      | `screens.md § User Journey step 2`                                                                              |
| TC024   | UAT                | The project-wide diagnostics tab is open showing errors and warnings                                                                                                         | Developer toggles warnings off from the toolbar                                                   | The list narrows to errors only, without losing track of which files still have warnings waiting                                                                                                                                                               | `screens.md § User Journey step 3`                                                                              |
| TC025   | UAT                | The project just woke from hibernation and diagnostic counts may be stale                                                                                                    | Developer opens the project diagnostics tab and glances at the sidebar file tree                  | A banner at the top of the tab notes counts may be stale, and a dimmed badge appears next to the affected file in the sidebar until re-verified                                                                                                                | `screens.md § User Journey step 4`                                                                              |
| TC026   | UAT                | A language server has flagged an error via the status-bar activity indicator                                                                                                 | Developer clicks the indicator, reads the popover message, then dismisses it                      | The message is read from the popover and the indicator's queued message is dismissed                                                                                                                                                                           | `screens.md § User Journey step 5`                                                                              |
| TC027   | UAT                | Developer is preparing a bug report                                                                                                                                          | Developer uses the "copy system specs" command (or, on Windows, records an ETW performance trace) | Supporting diagnostic info (system specs or a trace file) is produced to attach to the bug report                                                                                                                                                              | `screens.md § User Journey step 6`                                                                              |

---

## Coverage Notes

_(None — every `BR-###`/`SM-###`/`ALG-001`/`INT-001` code in `technical-spec.md` has at least one
tracing test case above. `DEC-###` and `DISC-###` families are empty for this feature (`Decision
Logic: None`; `Polymorphic Behavior: N/A — no diagnostics-specific behavior branch found`), so no
codes exist in those families to omit.)_
