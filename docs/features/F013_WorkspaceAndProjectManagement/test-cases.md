<!-- layout-exempt: rebuild-spec owns all docs/system|features|generated|flows paths -->

# Test Cases — F013_WorkspaceAndProjectManagement

**SIDECAR (v26.1.0):** this is a 5th, optional file alongside the 4 mandatory feature-spec files.
Its absence never blocks feature-spec promotion.

**Code Format**: `TC###` — 3-digit zero-padded, resets per feature.

**Citation-source split**: `UT`/`IT` rows cite a `BR-###`/`SM-###`/`DEC-###`/`DISC-###` code, a
`file:line`, or an `edge-cases.md` row. `UAT` rows cite a `screens.md`/`business-context.md`
section — never a bare code.

**CSV export**: out of scope v1 — Markdown is the sole output.

---

## Test Cases

| Test-ID | Type (UT\|IT\|UAT) | Given                                                                                                                                          | When                                                                                               | Then                                                                                                                                                                                        | Traces-to                                                                                                              |
| ------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| TC001   | UT                 | `OpenMode::NewWindow` is passed to open a workspace, no pre-existing `MultiWorkspace` for the target                                           | The open call is dispatched                                                                        | A brand-new OS window is created to host the workspace; new window/workspace DB rows are created on first serialize                                                                         | `DISC-001`                                                                                                             |
| TC002   | UT                 | `OpenMode::Add` is passed and an existing window's `MultiWorkspace` is resolvable                                                              | The open call is dispatched (deserialization-restore or a live add-or-activate)                    | The workspace attaches to that window's `MultiWorkspace` (`project_groups`/`retained_workspaces`); the existing window's DB session record is updated instead of a new window being created | `DISC-001`                                                                                                             |
| TC003   | UT                 | A project's `activity` is `Active`                                                                                                             | The sidebar rail renders that project's entry                                                      | The row shows at full opacity with no badge, and this project is never selected as a hibernation candidate                                                                                  | `DISC-003`                                                                                                             |
| TC004   | UT                 | A project's `activity` is `Warm` and it is not the selected entry                                                                              | The sidebar rail renders that project's entry                                                      | The row is visually unchanged except a muted label; the resource layer (LSP/worktree scanner) remains fully live                                                                            | `DISC-003`                                                                                                             |
| TC005   | UT                 | A project's `activity` is `Hibernated` and it is not currently re-indexing                                                                     | The sidebar rail renders that project's entry                                                      | The row shows a muted clock icon with tooltip "Hibernated — will wake when opened"                                                                                                          | `DISC-003`                                                                                                             |
| TC006   | IT                 | A project's `activity` is `Active` (its workspace is focused)                                                                                  | The memory-pressure fuse or any caller requests a transition to `Hibernated`                       | `set_activity` no-ops on the blocked `Active -> Hibernated` edge; the project remains `Active`                                                                                              | `BR-001`                                                                                                               |
| TC007   | IT                 | A project's `activity` is `Hibernated`                                                                                                         | A caller requests a transition to `Warm` (bypassing `activate()`)                                  | `set_activity` no-ops on the blocked `Hibernated -> Warm` edge; the only way out of `Hibernated` remains `MultiWorkspace::activate` back to `Active`                                        | `BR-001`                                                                                                               |
| TC008   | IT                 | A `Warm` project's idle timer fires while it has a running (non-terminated) debug session                                                      | `try_hibernate_resources` runs                                                                     | Hibernation is deferred and retried on `schedule_hibernate_retry`; the LSP stays up while the debug session is active                                                                       | `BR-002`                                                                                                               |
| TC009   | IT                 | A `Warm` project's idle timer fires while a buffer is dirty under a live (non-`Off`) autosave setting                                          | `try_hibernate_resources` runs                                                                     | Hibernation is deferred and retried rather than stopping the LSP mid-format-on-save                                                                                                         | `BR-002`                                                                                                               |
| TC010   | IT                 | A project's `activity` transitions `Hibernated -> Active`                                                                                      | `reconcile_resource_activity` runs                                                                 | `wake_resources` is called, restoring LSP/git/terminal resources that were actually torn down                                                                                               | `BR-003`                                                                                                               |
| TC011   | IT                 | A project's `activity` transitions `Warm -> Active` (a mere refocus, not a real hibernate)                                                     | `reconcile_resource_activity` runs                                                                 | `wake_resources` is NOT called, since nothing was torn down for a still-`Warm` project                                                                                                      | `BR-003`                                                                                                               |
| TC012   | IT                 | A worktree has not been granted trust (no `SecurityModal` confirmation on file/directory/parent scope)                                         | A buffer in a detected language is opened in that worktree                                         | The language server is not spawned (and certain git operations are blocked) until trust is explicitly granted                                                                               | `BR-004`                                                                                                               |
| TC013   | UT                 | A Dockerfile-backed devcontainer config sets `workspaceFolder` but omits `workspaceMount`                                                      | `validate_devcontainer_contents` runs                                                              | Validation fails with "workspaceMount and workspaceFolder must both be defined, or neither"                                                                                                 | `BR-005`                                                                                                               |
| TC014   | UT                 | A Docker-Compose-backed devcontainer config has no `service` set                                                                               | `validate_devcontainer_contents` runs                                                              | Validation fails with "must specify a connecting service for docker-compose"                                                                                                                | `BR-005`                                                                                                               |
| TC015   | UT                 | An Image-backed devcontainer config is supplied                                                                                                | `validate_devcontainer_contents` runs                                                              | Validation passes with no additional constraint                                                                                                                                             | `BR-005`                                                                                                               |
| TC016   | IT                 | A workspace that was `Active` loses focus in `MultiWorkspace::activate`                                                                        | The focus-loss is processed                                                                        | `activity` transitions `Active -> Warm`; `warm_since` is stamped and the hibernate timer is scheduled per the `hibernate_after` setting                                                     | `SM-001`                                                                                                               |
| TC017   | IT                 | A `Warm` project's workspace regains focus (manual click) before its hibernate timer fires                                                     | The refocus is processed                                                                           | `activity` transitions `Warm -> Active`; `hibernate_timers`/`warm_since` entries are cleared and `manually_woken_at` is stamped                                                             | `SM-001`                                                                                                               |
| TC018   | IT                 | A `Warm` project has had no manual wake and its per-workspace idle timer elapses                                                               | The timer fires                                                                                    | `activity` transitions `Warm -> Hibernated` and `try_hibernate_resources` runs                                                                                                              | `SM-001`                                                                                                               |
| TC019   | IT                 | A `Warm` project has been `Warm` for >= 60s and is not immune under `manually_woken_at`                                                        | The memory-pressure fuse selects it as a victim                                                    | `activity` transitions `Warm -> Hibernated` and `try_hibernate_resources` runs                                                                                                              | `SM-001`                                                                                                               |
| TC020   | IT                 | A project's `activity` is `Hibernated`                                                                                                         | The user clicks that project's sidebar entry, driving `MultiWorkspace::activate` -> `wake_project` | `activity` transitions `Hibernated -> Active` and `wake_resources` runs                                                                                                                     | `SM-001`                                                                                                               |
| TC021   | IT                 | A project has no `.devcontainer` config (`NoConfig`)                                                                                           | The `InitializeDevContainer` modal is completed                                                    | State transitions `NoConfig -> ConfigInitialized`                                                                                                                                           | `SM-002`                                                                                                               |
| TC022   | IT                 | A project's devcontainer state is `ConfigInitialized`                                                                                          | The project is opened/attached                                                                     | State transitions `ConfigInitialized -> ImageBuilding`                                                                                                                                      | `SM-002`                                                                                                               |
| TC023   | IT                 | A project's devcontainer state is `ImageBuilding`                                                                                              | The docker/podman build succeeds                                                                   | State transitions `ImageBuilding -> ImageBuilt`                                                                                                                                             | `SM-002`                                                                                                               |
| TC024   | IT                 | A project's devcontainer state is `ImageBuilding`                                                                                              | The docker/podman build fails                                                                      | State transitions back to `ConfigInitialized` and the failure is surfaced to the user                                                                                                       | `SM-002`                                                                                                               |
| TC025   | IT                 | A project's devcontainer state is `ImageBuilt`                                                                                                 | The editor runs/execs into the container and its lifecycle scripts                                 | State transitions `ImageBuilt -> ContainerRunning`                                                                                                                                          | `SM-002`                                                                                                               |
| TC026   | IT                 | Multiple `Warm` projects are each eligible (>= 60s `Warm`, not immune) and system memory is under threshold                                    | One `MEMORY_FUSE_POLL_INTERVAL` (30s) tick fires                                                   | At most one eligible project is hibernated this tick, not zero and not all eligible ones                                                                                                    | `crates/workspace/src/multi_workspace.rs:1769-1850`                                                                    |
| TC027   | IT                 | A project was just manually woken (`manually_woken_at` set)                                                                                    | The memory-pressure fuse runs within the same 30s poll interval                                    | That project is excluded from victim selection even though it would otherwise be `Warm`-eligible                                                                                            | `crates/workspace/src/multi_workspace.rs:283-310`                                                                      |
| TC028   | IT                 | A project has a valid `devcontainer.json` and Docker/Podman is unavailable or the build fails                                                  | The project is opened/attached, triggering `open_project_with_devcontainer`                        | The failure is surfaced to the developer (error toast/dialog) rather than leaving a stale or missing image silently in place                                                                | `crates/dev_container/src/devcontainer_api.rs:295-300`                                                                 |
| TC029   | IT                 | A project in the Welcome screen's recent list has had its folder moved or deleted since last session                                           | The developer clicks that recent entry                                                             | Opening fails to find the path and an error is surfaced instead of silently opening an empty/broken workspace                                                                               | edge-cases.md § Recent project's folder moved or deleted since last session                                            |
| TC030   | IT                 | A project's sidebar entry shows `Hibernated`, but `LspStore::hibernate`'s detached background task has not yet finished tearing down resources | Any action is taken against that project                                                           | The action may briefly still hit the still-live LSP connection — the "Hibernated" label is a lifecycle intent, not a real-time resource-state guarantee                                     | edge-cases.md § Sidebar shows a project as "Hibernated" but its language server/scanner teardown hasn't fully finished |
| TC031   | IT                 | A project has 2 git worktrees open                                                                                                             | The developer deletes one from the Worktree picker (`DeleteWorktree`, force=false)                 | The worktree is detached from the project's list, but the folder and its contents remain untouched on disk                                                                                  | edge-cases.md § Deleting a git worktree from the picker                                                                |
| TC032   | UAT                | The developer launches Zode and lands on the Welcome screen showing a recent-projects list                                                     | The developer clicks a recent project entry                                                        | The workspace opens showing that project's file tree in the Project panel and a new entry appears in the Project rail                                                                       | screens.md § User Journey steps 1-2                                                                                    |
| TC033   | UAT                | Two projects are open, each shown as an entry in the Project rail                                                                              | The developer clicks the other entry, or cycles with a keyboard shortcut                           | The clicked/cycled-to project becomes active in the window                                                                                                                                  | screens.md § User Journey step 3                                                                                       |
| TC034   | UAT                | A second open project sits idle in the background                                                                                              | Its idle timer elapses                                                                             | Its rail entry shows a small hibernated indicator                                                                                                                                           | screens.md § User Journey step 4                                                                                       |
| TC035   | UAT                | A project's rail entry shows the hibernated indicator                                                                                          | The developer clicks that entry                                                                    | It becomes active again and the Project panel/editor for that project responds normally within moments                                                                                      | screens.md § User Journey step 5                                                                                       |
| TC036   | UAT                | The developer has the Project panel open and navigates to a folder                                                                             | The developer creates a new file there                                                             | The new file opens immediately for naming                                                                                                                                                   | screens.md § User Journey step 6                                                                                       |
| TC037   | UAT                | The developer opens the Dev Container modal and picks a template, confirming the wizard                                                        | The project is reopened afterward                                                                  | The project's containerized environment builds and starts automatically                                                                                                                     | screens.md § User Journey step 7                                                                                       |
| TC038   | UAT                | The developer opens a folder Zode has never seen before                                                                                        | Zode needs to run language tooling or certain git operations against it                            | The developer is asked for explicit permission before any such tooling runs, protecting against silently executing code from an untrusted source                                            | business-context.md § What They Do bullet 7                                                                            |

---

## Coverage Notes

- `FR-001`, `FR-002`, `FR-003` — [NO_TEST_CASE] Requirements are exercised indirectly through their linked BR-###/SM-### test cases (e.g. FR-002 via BR-001/BR-002/BR-003/SM-001); the contract's expand-unit is BR/SM/DEC/DISC, not FR, so no separate row is added to avoid duplicate coverage of the same behavior.
- Decision Logic — [NO_TEST_CASE] `technical-spec.md` states "N/A — no user-facing decision logic beyond DISC-001/DISC-003 Polymorphic Behavior"; there is no DEC-### code in this feature to expand.
