[package]
name = "agent_sessions"
version = "0.1.0"
edition.workspace = true
publish.workspace = true
license = "GPL-3.0-or-later"

[lints]
workspace = true

[lib]
path = "src/agent_sessions.rs"
doctest = false

[dependencies]
anyhow.workspace = true
log.workspace = true
rusqlite.workspace = true
serde_json.workspace = true
tempfile.workspace = true
util.workspace = true

[dev-dependencies]
# Only to block on the one `#[ignore]`d live-CLI test (M7), which spawns the
# real `opencode` binary through `util::command::Command` -- an async API --
# from an ordinary, non-async `#[test]`.
smol.workspace = true
