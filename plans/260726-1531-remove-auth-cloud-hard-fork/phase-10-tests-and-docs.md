---
phase: 10
title: "Tests and docs"
status: completed — one open regression, see below
effort: "2d"
---

# Phase 10: Tests and docs

## Context Links

- [Settings scout](./reports/scout-settings-migration.md) §6 — the full list of breaking tests
- [Data-file scout](./reports/scout-data-files.md) §6 — the docs inventory
- [plan.md](./plan.md) — cross-plan dependency on the rebuild-spec artifacts

## Overview

**Priority:** P1 · **Build state:** 🔴 RED → GREEN for tests · **Depends on:** Phase 9

Fix the tests that the deletion legitimately broke, and sweep the docs — which are **not cosmetic here**: the mdbook preprocessor validates every action reference and every JSON settings snippet in `docs/`, and the `check_docs` CI job fails on any dangling one.

The `expected_namespaces` assertion deferred from Phase 8 is updated here, on purpose, only after the action list has been regenerated.

## Key Insights

- **`zed.rs:5077` `test_action_namespaces`** asserts `cx.all_action_names()` namespaces match a hardcoded list at `:5110-5198` including `"copilot"`, `"collab"`, `"zeta"`, `"agent"`, `"assistant"`, `"channel"`. It is the best canary for a leftover registered action. Update it **from the regenerated dump**, and treat an unexpected surviving namespace as a real finding.
- **`settings_store.rs:2871-2872`** literally asserts `user_schema_str.contains("\"auto_update\"")`. Hard fail once the key is gone.
- **`docs_preprocessor` validates docs against live code twice over**: `main.rs:370-390` compiles the settings schema and validates every labeled JSON block in `docs/`; `:254`, `:302`, `:193-200` resolve every `{#kb ...}` and `{#action ...}` reference and raise `PreprocessorError::ActionNotFound`. The `check_docs` CI job (`.github/workflows/run_tests.yml:636-680`) fails on either.
- `crates/docs_preprocessor/actions.json` is **not committed** — regenerated by `script/generate-action-metadata`. `load_all_actions` (`main.rs:622`) **panics** if it is missing when `CI` is set.
- `docs/src/SUMMARY.md` is **mandatory**: dangling links are a hard `mdbook build` failure. Lines `:13-28`, `:51-53`, `:165`, `:184`, `:186`, `:188` point at pages being deleted.
- The rebuild-spec artifacts under `plans/260726-1400-rebuild-spec/` and the generated `docs/system/` + `docs/generated/` describe a 240-crate workspace. They become factually wrong at Phase 4.

## Requirements

**Functional**
- `cargo nextest run --workspace` passes, with any failures traceable to the Phase 1 baseline rather than to this work.
- `mdbook build docs` succeeds; `check_docs` CI job green.
- No test silenced without justification.

**Non-functional**
- Tests deleted only when the feature they cover was deleted. Never `#[ignore]` to make the suite green.

## Architecture

```
regenerated action dump ─► expected_namespaces (zed.rs:5110-5198)
                        └─► docs {#kb} / {#action} sweep
                        └─► script/generate-action-metadata → actions.json (CI)

deleted settings keys   ─► settings_store.rs test fixtures + schema assertions
                        └─► docs JSON snippet validation

deleted crates          ─► docs/src/{ai,collaboration}/**, SUMMARY.md
                        └─► docs/system/*, docs/generated/*  (regenerate, Phase 11+)
```

## Related Code Files

**To modify — tests**
- `crates/zed/src/zed.rs` — `expected_namespaces` `:5110-5198`; `test_disable_ai_crash` `:5593`; `test_generate_keymap_json_schema_for_registered_actions` `:5064`
- `crates/settings/src/settings_store.rs` — `:2871-2872` schema assertion; fixtures `:2035`, `:2109`; test-only `impl Settings for AutoUpdateSetting` `:1634-1645`
- `crates/settings/src/keymap_file.rs` — `:1675`
- `crates/settings/src/settings_file.rs` — `:104-110`, `:127-133` (`.unwrap()` on `default.json` parse)

**To delete — docs (~27 files)**
- `docs/src/ai/*.md` (19), `docs/src/collaboration/*.md` (3)
- `docs/src/authentication.md`, `docs/src/extensions/agent-servers.md`, `docs/src/extensions/mcp-extensions.md`, `docs/src/update.md`
- **Keep** `docs/src/remote-development.md` — SSH remote dev survives

**To edit — docs (~30 files)**
- `docs/src/SUMMARY.md` (**mandatory**), `all-actions.md`, `reference/all-settings.md`, `reference/default-key-bindings.md` (all three generated — regenerate)
- `completions.md`, `configuring-zed.md`, `editing-code.md`, `extensions.md`, `extensions/{developing-extensions,languages,slash-commands}.md`, `getting-started.md`, `git.md`, `roles.md`, `terminal.md`, `telemetry.md`, `toolchains.md`, `troubleshooting.md`, `vim.md`, `visual-customization.md`, 5× `migrate/*.md`, 4× `development/*.md`, `languages/rego.md`

## Implementation Steps

### 10a. Tests

1. Regenerate the action metadata the docs pipeline needs:
   ```sh
   script/generate-action-metadata
   ```
2. Update `expected_namespaces` (`zed.rs:5110-5198`) **from the regenerated dump**, not by deleting the entries you expect to be gone. Remove `"agent"`, `"assistant"`, `"assistant2"`, `"auto_update"`, `"channel_modal"`, `"collab"`, `"collab_panel"`, `"copilot"`, `"edit_prediction"`, `"inline_assistant"`, `"agents_sidebar"`, `"zeta"` — **but only those the dump confirms are gone.** Anything still present is a finding: an action is still registered somewhere.
   > `"context_server"` should **remain** — the crate survives.
3. `settings_store.rs:2871-2872` — replace the `auto_update` schema assertion with a key that still exists.
4. Remove test fixtures referencing deleted keys: `settings_store.rs:2035`, `:2109`; delete the test-only `impl Settings for AutoUpdateSetting` `:1634-1645`.
5. `zed.rs:5593` `test_disable_ai_crash` — delete; the `disable_ai` key is gone.
6. Run the suite and diff against the Phase 1 baseline **by test name** (red team finding 7 — a summary-tail diff cannot support this phase's own acceptance bar):
   ```sh
   cargo nextest list --workspace > research/post-deletion-test-names.txt
   cargo nextest run --workspace --no-fail-fast --message-format libtest-json \
     > research/post-deletion-tests.json 2> research/post-deletion-tests.log

   # tests that vanished — every one must belong to a deleted crate
   comm -23 <(sort research/baseline-test-names.txt) <(sort research/post-deletion-test-names.txt)

   # tests that now FAIL but previously passed — must be empty
   ```
   Classify every delta into exactly one bucket: **(a)** disappeared with its crate, **(b)** deleted by us on purpose, **(c)** newly failing. Bucket (c) must be empty. **Do not `#[ignore]` anything to close a gap.**

### 10b. Docs

7. Delete the ~27 files listed above. **Keep `remote-development.md`.**
8. `docs/src/SUMMARY.md` — remove every link to a deleted page (`:13-28`, `:51-53`, `:165`, `:184`, `:186`, `:188`). Dangling links are a hard build failure.
9. Sweep for dangling action/keybinding references across all remaining docs:
   ```sh
   rg -n '\{#kb |\{#action ' docs/src/ | grep -Ef <(sed 's/^/:/' research/removed-actions.txt)
   ```
   Rewrite or remove each hit.
10. Sweep for JSON settings snippets containing deleted keys — the preprocessor validates them against the live schema:
    ```sh
    rg -n '"agent"|"language_models"|"edit_predictions"|"collaboration_panel"|"auto_update"|"calls"' docs/src/
    ```
11. Regenerate the three generated docs: `all-actions.md`, `reference/all-settings.md`, `reference/default-key-bindings.md`.
12. Build the book:
    ```sh
    mdbook serve docs        # or: mdbook build docs
    ```
    Per `docs/README.md:14`, `script/generate-action-metadata` must have run first (step 1).
13. Update `docs/README.md` and `CONTRIBUTING.md` where they reference removed subsystems.

### 10c. Generated spec artifacts

14. Mark the rebuild-spec output stale. `docs/system/overview.md`, `docs/system/architecture.md`, `docs/generated/feature-list.md` etc. all describe the deleted subsystems (F006 extensions aside, F007/F008 collab, F013 AI agent, F019 voice call, F020 edit prediction, F021 LLM switching, F022 auto-update are all now fiction).
15. Schedule regeneration via `/tkm:rebuild-spec` **after Phase 11 goes green** — running it against a red workspace is wasted effort. Add a note to `plan.md`'s dependency table.

### 10d. Repo hygiene

16. `.rules` (symlinked as `CLAUDE.md`) has a **"Crash Investigation"** section referencing `.factory/prompts/crash/*` and `script/sentry-fetch` — all dead after Phase 6. Remove the section and the dead scripts.
17. `typos.toml`, `lychee.toml`, `.repomixignore` — check for references to deleted paths.

## Todo List

- [x] 10a `script/generate-action-metadata` run (1,163 actions)
- [x] 10a `expected_namespaces` generated from the dump; **2 survivors found and removed** — see log
- [x] 10a `settings_store.rs` schema assertion — already fixed in Phase 9 (repointed to `vim_mode`)
- [x] 10a fixtures — already handled in Phase 9
- [x] 10a `test_disable_ai_crash` **KEPT** — Phase 9 kept `disable_ai`; deleting it would have been wrong
- [x] 10a Test diff by name; (a) 1,660 / (b) 19 all accounted for; **(c) is NOT empty — 1 regression, unfixed and unhidden**; nothing `#[ignore]`d (verified: 0 added)
- [x] 10b 26 docs deleted; `remote-development.md` kept
- [x] 10b `SUMMARY.md` fixed; `worktree-trust.md` re-parented rather than orphaned
- [x] 10b `{#kb}`/`{#action}` sweep clean (alias-aware)
- [x] 10b JSON snippet sweep clean — **the preprocessor caught 3 real errors**
- [x] 10b `all-actions.md` + keybinding tables are templated at build time; `all-settings.md` hand-edited
- [x] 10b **`mdbook` is not installed here**; ran the preprocessor directly over all 144 chapters instead — that is the component `check_docs` enforces. Exits 0. A real `mdbook build` is still unverified
- [x] 10c all 20 spec artifacts marked stale with the specific fiction named; regeneration deferred
- [x] 10d section, `script/sentry-fetch` and `.factory/prompts/crash/` removed; 7 stale `typos.toml` exclusions

## ⚠ Open regression (bucket (c) is NOT empty)

`editor inlays::inlay_hints::tests::test_refresh_requested_multi_server` **fails**. It is the only
failure in 4,001 tests. It has not been hidden, skipped or `#[ignore]`d.

What is established:

| | |
|---|---|
| Deterministic | 5/5 runs fail identically |
| Failure | `Both servers should have hints initially. Got: ["server_b_1"]` — server A never produces hints |
| Passed at baseline | **Yes** — verified by running it in a worktree at `pre-structural-cut` |
| `git bisect` | first bad commit is **`c3e2ac3`, the Phase 4 atomic cut** — not any later phase |
| `crates/editor` | **zero changes** across the whole fork (`git diff pre-structural-cut -- crates/editor/` is empty) |
| Sibling tests | 32 of 33 `inlay` tests pass; only the two-server case fails |

**Why the baseline could not answer this on its own** — red team finding 7, confirmed in practice.
`research/baseline-tests.log` is 5,956 bytes for 5,674 tests: a summary tail, with only 36 `PASS`
lines. It records that 21 tests failed (all `collab` Postgres tests needing a database that was not
running — all since deleted) but cannot show whether any *specific* test passed. Answering this
required rebuilding the baseline in a worktree.

**Most likely mechanism, not yet proven:** `crates/copilot` registered an LSP adapter for every
language. Removing it changed what `LanguageSettings::resolve_language_servers` resolves for a
language with two registered adapters, so the test's server A ("rust-analyzer") never starts. The
fix is in `editor`/`language` server-resolution territory — the crate this plan deliberately kept at
zero change — so it is **handed to Phase 11** rather than patched blind here.

## Success Criteria

- `cargo nextest run --workspace` — all deltas from baseline explained by intentional deletions.
- `mdbook build docs` succeeds with zero dangling links.
- `check_docs` CI job green.
- `test_action_namespaces` passes with a list derived from the real action dump.
- No `#[ignore]` added anywhere in this phase.

## Risk Assessment

| Risk | Impact | Mitigation |
|---|---|---|
| `expected_namespaces` edited to "make it pass" | Loses the canary; a leftover registered action ships | Step 2 requires deriving from the dump and investigating surprises |
| A test `#[ignore]`d to close the suite | Green build hiding a real regression — explicitly forbidden by the project rules | Step 6 states it; success criteria assert it |
| `actions.json` missing in CI | `load_all_actions` **panics** when `CI` is set | Step 1 regenerates it; confirm the CI step still runs `script/generate-action-metadata` |
| Docs JSON snippet left with a deleted key | `check_docs` CI failure, discovered late | Step 10 sweeps explicitly |
| `remote-development.md` deleted by reflex with the other cloud docs | Documentation lost for a feature that survives | Called out in three places in this phase |
| Stale `docs/system/*` treated as truth by a future session | Wrong decisions built on fiction | Step 14/15 mark and schedule regeneration |

## Security Considerations

`docs/src/telemetry.md` and `legal/privacy-policy.md` describe data collection that no longer happens. Leaving them unchanged would be a **false statement to users** about what the software does. Update both — this is an accuracy obligation, not housekeeping. `legal/subprocessors.md` and `legal/third-party-terms.md` likewise reference vendors (LiveKit, LLM providers) that are gone.

## Next Steps

Phase 11 — the green gates and runtime privacy verification. The build returns to green here.
